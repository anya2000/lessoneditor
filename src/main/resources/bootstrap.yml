spring:
  application:
    name: LessonEditor

  profiles:
    active: dev    

  cloud:
    config:
      env: default # optional
      label: master # optional
      uri: http://localhost:9000 # remote configuration server uri
      failFast: false # true = cause the server to fail if remote configuration init fails

